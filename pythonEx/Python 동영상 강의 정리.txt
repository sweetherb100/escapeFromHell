2017.03.26. 1주차
powershell
#
'''
'''
print
power=2**10 (** 2개)
REPL =Read-Eval- Print Loop
python -> exit()

graphic shell/ command line shell
(shell : 일종의 interface)

[Shell 사용법]
pwd : print working directory
ls : list segment
cd : change directory
cp : copy
rm : remove
[TIP] cd JI까지 누른다음에 tab 누르면 그 중에 선택

[if else, else if]
elif : else if

[함수 만들기]
def print_sqrt():

return 여러값으로 반환하려면 쉼표로 구분
그 후 print('해는 {} 또는 {}' .format(r1,r2)) 이런 식으로 return 값 받아오기

사용자입력하기 위한 명령어 : 
input()
print 명렁어 중 end param :
다음 줄로 넘어가는 것이 아니라 바로 그 끝 줄에 end 안의 내용이 들어감.

[리스트]
리스트 안에 특정 변수가 있는지 없는지 체크하렴녀 n in list를 활용 (true,false로 반환)

import urllib.request 활용 예제 굿




